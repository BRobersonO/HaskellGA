module Crossover where

import CustomTypes

crossover :: (Chromosome, Chromosome) -> Chromosome
crossover (parent1, parent2) = undefined

addToPop :: Chromosome -> Population -> Population
addToPop newChromo pop  = undefined

addThree a b c = a + b + c