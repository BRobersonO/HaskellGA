module Selection where

import CustomTypes

select :: (Chromosome, Chromosome)
select = undefined

tournament :: Int -> (Chromosome -> Int) -> [Chromosome] -> Chromosome
tournament size fitFunc competitors = undefined

pickOne :: Chromosome
pickOne = undefined